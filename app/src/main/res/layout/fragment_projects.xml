<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.ic_tea.david.copo.ProjectsActivityFragment"
    tools:showIn="@layout/activity_projects">
    <!--TextView
        android:id="@+id/am_select_category_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:elevation="8dp"
        android:gravity="center_horizontal"
        android:padding="4dp"
        android:text="@string/select_competence_mesage"/-->

    <ListView
        android:id="@+id/am_main_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/ma_button_layout" />

    <LinearLayout
        android:id="@+id/ma_button_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:padding="8dp"
        android:orientation="horizontal">
        <Button
            android:id="@+id/ma_share_all_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="shareAllClick"
            android:padding="4dp"
            android:text="@string/share_all"
            android:textSize="16sp"
            android:layout_weight="1"/>

        <Button
            android:id="@+id/am_add_new_project_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:onClick="addNewProjectClick"
            android:padding="4dp"
            style="@style/Base.Widget.AppCompat.Button.Colored"
            android:text="@string/add_project"
            android:textSize="16sp"
            android:layout_weight="1"/>
    </LinearLayout>


</RelativeLayout>
